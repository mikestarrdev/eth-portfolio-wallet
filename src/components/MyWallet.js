import React from "react";
import { useState, useEffect } from "react";
import { ethers } from "ethers";

function MyWallet({ provider, signer }) {
  const [walletHoldings, setWalletHoldings] = useState({});
  useEffect(() => {
    
  }, []);

  return <div></div>;
}

export default MyWallet;
